# generated for sxfgssever local LAN access
#
# Allow: localhost + 192.168.109.0/24
# Deny:  everything else

# Unix socket
local   all             all                                     scram-sha-256

# IPv4 localhost
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 localhost
host    all             all             ::1/128                 scram-sha-256

# LAN allowlist (192.168.109.0/24)
host    all             all             192.168.109.0/24        scram-sha-256

# LAN allowlist (192.168.107.0/24)
host    all             all             192.168.107.0/24        scram-sha-256

# Reject the rest (defense in depth)
host    all             all             0.0.0.0/0               reject
host    all             all             ::/0                    reject
